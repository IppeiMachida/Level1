<% if post.favorited_by?(current_user) %>
   <%= link_to "Unlike", post_favorites_path(post.id), method: :delete, remote: true %>
<% else %>
   <%= link_to "Like", post_favorites_path(post.id), method: :post, remote: true %>
<% end %>